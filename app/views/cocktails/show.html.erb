<h1><%= @cocktail.name %></h1>
<div class="ingredients">
  <% @cocktail.doses.each do |dose| %>
    <p>
      <i class="fa fa-check-circle-o"></i> <%= dose.description %> of <%= dose.ingredient.name %> · <%= link_to "Delete", cocktail_dose_path(dose.cocktail, dose), method: :delete, data: { confirm: "Are you sure?" } %>
    </p>
  <% end %>
</div>

<h2>Add a new ingredient</h2>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient, collection: Ingredient.all.map { |ingredient| ingredient.name } %>
  <%= f.input :cocktail, as: :hidden, input_html: { value: @cocktail.name } %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
